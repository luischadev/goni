---
interface Props {
  eyebrow?: string;
  title: string;
  description?: string;
  align?: "left" | "center";
  as?: "h1" | "h2";
}

const { eyebrow, title, description, align = "left", as = "h2" } = Astro.props;
const alignment = align === "center" ? "text-center mx-auto" : "text-left";
const Heading = as;
---

<div class={`max-w-2xl ${alignment}`}>
  {eyebrow && <p class="eyebrow">{eyebrow}</p>}
  <Heading class={`font-display ${as === "h1" ? "text-4xl md:text-6xl" : "text-3xl md:text-4xl"} mt-6 leading-[1.1]`}>
    {title}
  </Heading>
  {description && (
    <p class="mt-6 text-base md:text-lg text-fg-muted leading-relaxed max-w-xl">
      {description}
    </p>
  )}
</div>
